// SORT and NEST contracts by expiration date and strike price

export const selectContracts = (contracts) => {
  const selected = {
    // expiration: {
    //   strike: {
    //     put: id
    //     call: id
    //   }
    //   ...
    // }
  }

  const ids = Object.keys(contracts);

  ids.forEach(id => {

  })
}